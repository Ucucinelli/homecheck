## Spring view resolver set up
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp



# JWT settings
jwt.secret = abcdefghijklmnopqrstuvwxyz123
jwt.defaultAccessTokenExpirationSeconds = 3600
jwt.defaultRefreshTokenExpirationSeconds = 2592000


# Tomcat settings
server.port = 9999
server.shutdown=graceful
server.servlet.context-path=/api

# Handle reverse proxy headers
# DOMANDARE SE SERVE SOLO IN LOCALE O ANCHE CON CLOUD
# server.use-forward-headers=true
server.forward-headers-strategy=NATIVE
server.tomcat.redirect-context-root=false

# Spring Security settings
spring.security.user.name = admin
spring.security.user.password = pse2020

# Mongo settings
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=pse

# Springdoc OpenAPI settings
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.packagesToScan=it.ecubit.homecheck.web
springdoc.paths-to-match=/login, /refresh, /diasu/pazienti/**, /aslvt/pazienti/**, /aslvt/dispositivi/**, /utenti/**, /nazioni/**

# PSE custom settings
server.error.whitelabel.enabled=false
spring.mvc.throw-exception-if-no-handler-found=true
spring.resources.add-mappings=false

DEFAULT_PAGE_SIZE=20
DEFAULT_FRONTEND_LOCALE_LANGUAGE=ITALIAN
pse.storageBaseUrl=http://ec200601.seewebcloud.it:8989/api/archivio

# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Max file size
spring.servlet.multipart.max-file-size=200MB
# Max request size
spring.servlet.multipart.max-request-size=210MB
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=10MB

#Config Server settings
spring.config.import=optional:configserver:http://localhost:8888
#spring.profiles.active=

# Eureka Server settings
eureka.client.service-url.default-zone=http://localhost:8761/eureka

spring.sleuth.sampler.probability=1.0

spring.mvc.hiddenmethod.filter.enabled=true

# User
user.domain=hida

